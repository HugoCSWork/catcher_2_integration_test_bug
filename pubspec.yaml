name: integration_test_defect
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  catcher_2: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test: 
    sdk: flutter
  flutter_lints: ^3.0.0

dependency_overrides:
  catcher_2:
    git:
      url: https://github.com/ThexXTURBOXx/catcher_2.git

flutter:
  uses-material-design: true
